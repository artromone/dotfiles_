local function config() end

local function opts()
    return { show_icons = true, leader_key = '<F9>', }
end

return {
    "otavioschwanck/arrow.nvim",
    -- event = "",
    -- dependencies = {},
    -- config = config,
    opts = opts
}
